optimiDefaultConstraint <-
function (constraint) {
  switch (EXPR = constraint,
  positive = {
  str = "exp" },
  zeroone = { 
  str = "sigmoid" },
  bounded = {
  str = "sigmoidab" }) 
  return (str)
}
